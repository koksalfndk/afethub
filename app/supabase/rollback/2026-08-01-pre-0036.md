# Canlı uygulama öncesi durum kaydı — 01-08-2026

Proje: `sdowkjbqaqoqmlovnlfh` (AfetHUB Web, eu-central-1, Postgres 17.6).
Bu dosya `0036`, `0037` ve `0038` canlıya uygulanmadan HEMEN ÖNCEKİ durumu kaydeder.
Amacı geri dönüş: hangi tanımın değiştiği ve neyin eski hâlinin ne olduğu.

## Satır sayıları (uygulama öncesi)

| tablo | satır |
|---|---|
| disasters | 7 |
| needs | 31 |
| submissions | 7 |
| locations | 9 |
| audit_log | 34 |

## disaster_overview — uygulama öncesi sütun listesi (28 sütun)

    id, slug, name, region, province, type, status, opened_at, updated_at,
    volunteers, on_shift, is_demo, active_needs, completed_needs,
    pending_submissions, pending_units, verified_submissions, delivery_points,
    situation, legacy_slugs, opened_by_org_id, opened_by_community,
    community_confirmed_at, districts, settlements,
    required_total, verified_total, remaining_total

Tanımın md5'i: `181b5246e0e19f879b91bbfa1790277a`
Eski tanımın tamamı `supabase/migrations/0035_disaster_overview_amounts.sql` içinde
birebir duruyor; geri dönüş için o dosya yeniden çalıştırılır.

## audit_is_public() — uygulama öncesi hâli

md5: `e294982d9164b6ccff303a82bdce4bee`

Canlı liste, depodaki `0016_community_reports_and_public_feed.sql` listesinden İKİ
aksiyon eksikti (kayma):

    'Operasyon açıldı'
    'Afet kaydı güncellendi'

Ölçüldü: `audit_log` içinde bu iki aksiyona sahip **sıfır** satır var, dolayısıyla
0036/0038 ile listenin depo hâline hizalanması mevcut hiçbir kaydı yeni görünür
yapmıyor. İstemci tarafındaki `PUBLIC_AUDIT_ACTIONS` (repo.ts) ikisini zaten
içeriyordu; yani kayma SQL tarafındaydı ve bu uygulama onu kapatıyor.

Geri dönüş: `0016`'daki `audit_is_public` gövdesi yeniden çalıştırılır.

## Değişecek RLS politikaları

`disasters`, `needs`, `audit_log` üzerindeki 6 politika (disasters_read,
disasters_coord_insert, disasters_coord_update, needs_read, needs_coord_write,
audit_read) **DEĞİŞTİRİLMİYOR**. 0036-0038 yalnızca yeni tablolara politika ekliyor.

## disasters / needs — eklenen sütunlar

Yalnızca ekleme, hiçbir sütun silinmiyor veya tip değiştirmiyor:

    disasters += operation_stage, operation_stage_note,
                 operation_stage_set_at, operation_stage_set_by
    needs     += featured_rank

Geri dönüş (veri kaybettirmeyen değil — bu sütunlar veri taşıyorsa geri alma
VERİ SİLER, bu yüzden yalnızca gerçekten gerekliyse):

    alter table disasters drop column if exists operation_stage_set_by;
    alter table disasters drop column if exists operation_stage_set_at;
    alter table disasters drop column if exists operation_stage_note;
    alter table disasters drop column if exists operation_stage;
    alter table needs     drop column if exists featured_rank;
    drop type if exists operation_stage;

Tercih edilen geri dönüş yolu: sütunları BIRAKMAK ve yalnızca
`0035_disaster_overview_amounts.sql` ile görünümü eski hâline döndürmek. Sütunları
kullanan bir ekran yoksa varlıkları zararsızdır.

## Yedek / PITR

Supabase MCP araç setinde yedek veya PITR durumunu okuyan bir araç yok; bu oturumdan
**doğrulanamadı**. Uygulanan işlemlerin hiçbiri destructive değil (yalnızca CREATE ve
ADD COLUMN), mevcut hiçbir tablo, sütun, satır veya politika silinmiyor.
